---
 - hosts: all
   tasks:
        - dnf:
                name: "{{ item }}"
                state: present
          with_items:
                - docker
                - docker-compose
        - systemd:
                name: docker.service
                enabled: yes
